<g:renderDomain template="editor"
                domain="${referencedDomain}" name="${name}"
                widgets="${widgets}" style="${style}"
                value="${value}"
                except="['id']"/>